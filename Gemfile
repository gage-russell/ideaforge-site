# Gemfile for local Jekyll development matching GitHub Pages
# Uses the github-pages gem to pin Jekyll and plugins to the same versions GitHub uses
source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
# Ruby 3 requires webrick as a separate dependency for `jekyll serve`
gem "webrick", "~> 1.7"
